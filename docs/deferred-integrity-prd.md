# Deferred integrity work

Status: deferred after the 2026-08-16 integrity safe-stop.

This document records future work. It does not authorize production changes.

## 1. Future-wave goal

Remove the remaining confirmed integrity, installer, configuration, and boundary gaps without reopening the accepted safe-stop. Preserve local bytes, explicit ownership, fail-closed state classification, and honest user-visible outcomes.

## 2. Current safe checkpoint

The integration candidate contains the accepted Bridge series through `273b01b`, D10 through `58f1388`, and runtime updater through `65036f6`. These series cover D3, D4, D9, D10, M9, M10, M11, M16, M17, and X1. The common integration gate still decides whether this candidate becomes the safe-stop.

D10 intentionally retains uncertain cleanup debt. That debt can grow disk use. A future collector must delete an object only after it proves ownership and liveness.

## 3. Proven integrity class definitions

These names are review vocabulary. Recording a class here does not create new implementation scope. The anchors below refer to base `a5a78e9`.

### CARD-OWNERSHIP — open

**Definition.** A writer may publish a Card only while it still owns the create or merge decision. A preflight existence check is not ownership.

**Current anchor.** `agent/tools/write_file.ts:28-39` checks whether the target is an existing Card. The check passes when the path is absent. Execution then yields at `agent/tools/write_file.ts:62` before `writeFile()` opens the target with truncate semantics at `:63`. The Card-aware path instead locks, rereads, merges, and publishes at `agent/tools/write_card.ts:332-407`.

**Loss scenario.** The target is absent during `isExistingCard()`. Another writer creates a Card before `write_file` publishes. `write_file` then truncates that foreign Card and destroys unknown frontmatter fields and its body.

**Minimal repro.** Start `write_file.execute()` for an absent `cards/contacts/alice.md`. Before awaiting its promise, create that path with `phone: +998900000000` and `Foreign body`. On `a5a78e9`, the call returns `ok: true`; the final file is only `# Model replacement`, and both the unknown field and foreign body are gone. Existing-Card and symlink tests at `scripts/vault-tools-paths.test.ts:58-84` and `scripts/review-fixes.test.ts:139-161` do not exercise this check-then-write interleaving.

### WRITE-FILE-ATOMICITY — open

**Definition.** A process crash during replacement must leave the target as one complete old or new generation. It must never expose a missing, empty, truncated, or mixed target.

**Current anchor.** `agent/tools/write_file.ts:62-63` calls direct `writeFile()` on the target. Opening an existing target truncates it before the full payload is written.

**Loss scenario.** `write_file` replaces `vault/CORE.md`. `SIGKILL` lands after truncate and before the complete write. The only target path now contains partial bytes, so the next Turn reads damaged or empty CORE.

**Minimal repro.** Fill `CORE.md` with 64 MiB of `A`, start `write_file` with 64 MiB of `B`, wait until the target size differs from 67,108,864, then send `SIGKILL`. On `a5a78e9`, the observed and surviving target sizes are both `0/67108864` bytes.

The Wave B code fix is a separate commit for later integration. This definition records only the proven class and its base anchor.

### DATA-DURABILITY — ALREADY CLOSED

**Definition.** A successful atomic replacement must survive power loss. A complete rename is not durable until the temporary file and the containing directory have crossed their persistence barriers.

**Historical evidence.** Findings D1, M5, and M7 proved this class when the shared writer performed temp-write plus rename without file or directory `fsync`, while PERSONA, interview, and the GWS secret used direct writes. A successful call could therefore disappear or restore the old directory entry after power loss. The review did not perform a real power cut; its minimal proof was the completed write/rename trace with no durability syscall.

**Current closure anchor.** The synchronous protocol writes and syncs the temporary file before rename, then syncs the parent directory at `agent/lib/fs-atomic.ts:412-431`. The asynchronous protocol applies the same barriers at `agent/lib/fs-atomic.ts:519-539`. The historical M5/M7 callers now use that writer at `scripts/lib/core-interview.ts:110-112`, `scripts/lib/menu/character.ts:147-152`, and `scripts/lib/menu/gws.ts:344-347`.

**Minimal regression repro.** Inject one failure after every protocol step. Before rename, the old target must remain. After rename but before directory confirmation, the new target may be published, but the call must fail with `EATOMIC_WRITE_DURABILITY`. `agent/lib/fs-atomic.test.ts:132-167` is the example anchor. The child-process crash anchor at `agent/lib/fs-atomic.test.ts:840-879` kills at write, file-sync, and rename boundaries and accepts only a complete old or new target.

`ALREADY CLOSED` applies to the historical D1/M5/M7 class and these migrated callers. It does not claim that every writer uses the canonical helper, and it does not open a new Wave B production task.

## 4. Deferred requirements

`P1`, `P2`, and `P3` below preserve the review priority. `Owner` blocks implementation until the owner fixes the product contract. `Accepted` means no implementation without a new owner decision.

### Updater and installer

`INSTALL_DIR` paths containing spaces remain deferred. The variable is documented at `docs/install.md:98`, while deploy units use unquoted `ExecStart` path placeholders, so systemd can tokenize one path as multiple arguments. Return before claiming paths-with-spaces support. Acceptance: systemd-escape or quote every path token, then test a synthetic installation path containing spaces.

| ID  | Priority            | Confirmed problem and evidence                                                                                                                            | User impact                                                   | Return condition                                        | Acceptance criteria                                                                                                                                   | Required tests                                                                                                                        | Dependencies and change risk                                                                                       |
| --- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| D11 | P1                  | Custom-layer commits with two renames at `scripts/lib/custom-layer.ts:763-779`. A stop between them removes the canonical path.                           | Startup can see no custom layer or a split tree and manifest. | Resume before any custom-layer format or writer change. | Store immutable tree-plus-manifest generations. Select one generation with one atomic pointer change. Readers see the complete old or new generation. | Example: old/new generation selection. Fault test: kill after every filesystem transition. No PBT.                                    | Depends on one updater owner contract from M19. Risk: migration can orphan an active generation.                   |
| D12 | P1                  | Repair uses two moves at `repair.sh:283-289`. A stop after the first move removes the install path.                                                       | Repair can leave the service with no runnable installation.   | Resume before enabling or documenting repair.           | Reuse version-store. Every interruption leaves the old or new installation addressable. Recovery is idempotent.                                       | Example: successful repair and retry. Crash test after each move or pointer transition. No PBT.                                       | Depends on M19 and the version-store ownership contract. Risk: shell and TypeScript owners can disagree.           |
| D13 | P1                  | `install.sh:1-2` enables `errexit`, while the optional `gh` block at `install.sh:624-658` is not fully isolated.                                          | An optional tool failure aborts the required install.         | Resume before the next installer release.               | Put the whole `gh` bootstrap behind one best-effort boundary. Emit one warning. Complete the required install. Leave partial package state safe.      | Shell examples for missing package manager, download failure, install failure, and success. Fault injection for each command. No PBT. | Independent of runtime. Risk: shell error propagation differs across supported systems.                            |
| M19 | P1                  | `scripts/lib/update-safety.ts:187-245` and `scripts/lib/version-store.ts:441-525` use different lock owners. One legacy owner can acquire both contracts. | Concurrent update paths can mutate one installation together. | Resume before adding another updater or repair caller.  | One PID-plus-nonce protocol serves all callers. Acquire allows one live holder. Release checks the exact token. Crash takeover is explicit.           | Example: acquire/release/token mismatch. Stateful PBT for acquire, release, crash, and takeover. Child-process crash test.            | Must precede D11 and D12. Risk: a bad takeover rule can deadlock updates or admit two holders.                     |
| M20 | P2; release blocker | `install.sh:684` downloads floating `uv/install.sh`. `install.sh:701` pins `nvm` without a digest.                                                        | A changed upstream installer runs without repository review.  | Mandatory before any new public installer release.      | Pin each downloaded artifact and its SHA-256. Verify bytes before execution. Fail closed on mismatch.                                                 | Examples for correct digest, wrong digest, truncation, and unavailable download. No PBT.                                              | Depends on release ownership and a digest-update process. Risk: platform-specific artifacts can make pins brittle. |

### Security and product boundaries

| ID  | Priority            | Confirmed problem and evidence                                                                                                                                                                                                                                 | User impact                                                              | Return condition                                                                                     | Acceptance criteria                                                                                                                         | Required tests                                                                                               | Dependencies and change risk                                                                             |
| --- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| S2  | P1                  | `agent/lib/security-gate.ts:514` and the skill's Python gate omitted `BEARER` from secret-key detection. The Python copy is deleted; the canonical key set is `agent/skills/security-defense/outbound-sensitive-keys.json`, read by the TypeScript gate alone. | A bearer value can leave the protected output path unchanged.            | Resume with the next outbound-gate policy change.                                                    | One canonical secret-key set feeds the gate. Every project bearer key is redacted. Unknown data does not bypass the gate.                   | Example with synthetic `ASSISTANT_BEARER`. PBT: every known secret key always removes its dummy value.       | Independent. Risk: broad matching can redact ordinary prose.                                             |
| S4  | P1                  | `scripts/poller/control.ts:440-449` checks only the allowlist. `scripts/lib/menu/index.ts:209-235` and `:386-407` do not require a private chat.                                                                                                               | A group member can open state-changing menu and callback flows.          | Resume before wider Telegram menu use.                                                               | Apply one private-chat gate before menu dispatch and callbacks. Reject stale group callbacks. Keep separately approved group-safe commands. | Examples for private, group, channel, missing type, and stale callback. No PBT.                              | Needs an explicit list of group-safe commands. Risk: a broad gate can block intended group commands.     |
| S1  | Owner; P3           | Absolute paths are allowed by `agent/tools/read_file.ts:15-19` and `agent/tools/write_file.ts:52-64`. Host access is promised at `agent/instructions.md:74-77` and `docs/security.md:64-66`.                                                                   | The current host-wide power may exceed a future least-privilege promise. | The owner chooses host-wide or capability-root access.                                               | Record the contract in an ADR. If narrowed, constrain `bash` and file tools together. Provide a typed configuration path.                   | After the decision: examples for inside/outside roots. PBT only for the chosen root resolver.                | Owner decision required. Risk: a Vault-only patch breaks the documented product and leaves `bash` wider. |
| S3  | P3; latent          | `scripts/lib/notice.ts:25-45` gates only `text` and `caption`. Button labels are unchanged, but current labels are static.                                                                                                                                     | Future dynamic labels could expose content outside the current gate.     | Resume when a dynamic user-derived button label is introduced or the visible-field contract changes. | Define the finite user-visible field set. Gate each dynamic field. Avoid generic recursive rewriting.                                       | Examples for each approved visible field and one static label. PBT only if a finite field schema is adopted. | Depends on a UI-field contract. Risk: recursive redaction can corrupt callback structure.                |
| S5  | Accepted            | ADR evidence: `docs/adr/0005-lethal-trifecta-deterministic-separation.md:36-40`. Bash inherits process environment by owner choice.                                                                                                                            | A shell command has the same environment reach as its process.           | Only a direct owner decision reopens it.                                                             | New ADR defines a narrower environment contract and compatibility plan.                                                                     | None while accepted. Add explicit allow/deny examples if reopened.                                           | Accepted risk. A partial filter can create false assurance.                                              |
| S6  | Accepted            | ADR evidence: `docs/adr/0005-lethal-trifecta-deterministic-separation.md:36-40`. Outbound Gate remains fail-open by owner choice.                                                                                                                              | Gate faults can allow output rather than stop it.                        | Only a direct owner decision reopens it.                                                             | New ADR defines fail behavior, user messaging, and recovery.                                                                                | None while accepted. Fault tests for detector unavailable or throwing if reopened.                           | Accepted risk. Fail-closed behavior can block all delivery.                                              |
| S7  | Accepted limitation | `docs/adr/0006-web-surface-passes-the-inbound-gate.md:198-229` documents a list-based detector and English Telegram coverage.                                                                                                                                  | Unlisted or non-English patterns can remain undetected.                  | Reopen only with a new detector contract, not another ad hoc pattern list.                           | Define supported languages, threat classes, evaluation corpus, and measured limits.                                                         | Corpus examples and false-positive/false-negative evaluation. No generic PBT.                                | Accepted limitation. More patterns do not close the class.                                               |
| S8  | Accepted            | ADR evidence: `docs/adr/0005-lethal-trifecta-deterministic-separation.md:36-40`. Documents, userbot input, and browser output remain raw by owner choice.                                                                                                      | These sources bypass the outbound text gate contract.                    | Only a direct owner decision reopens it.                                                             | New ADR assigns trust and transformation boundaries per source.                                                                             | None while accepted. Source-specific examples if reopened.                                                   | Accepted risk. Generic filtering can destroy source fidelity.                                            |

### Configuration, runtime, and data

| ID  | Priority       | Confirmed problem and evidence                                                                                                                                                                            | User impact                                                            | Return condition                                                         | Acceptance criteria                                                                                                                                                   | Required tests                                                                                                                                                                | Dependencies and change risk                                                                                     |
| --- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| R1  | P1             | `agent/instrumentation.ts:43-58` repairs `TZ` but leaves raw `ASSISTANT_TIMEZONE` consumers exposed.                                                                                                      | One process falls back while another throws for the same invalid zone. | Resume before timezone configuration changes.                            | One resolver returns a validated IANA zone or `UTC`. Every process consumes it.                                                                                       | Examples for valid, empty, and invalid zones. PBT: arbitrary strings always resolve to a valid zone. Seed `18801`.                                                            | Coordinate with C4. Risk: changing fallback changes card dates and schedules.                                    |
| R2  | P1             | `agent/provider.ts:28-75` accepts `Number()` results without rejecting `NaN`, zero, negative, or fractional windows.                                                                                      | Invalid limits can disable or corrupt context trimming.                | Resume before exposing context-window configuration.                     | Parse one positive safe integer. Reject every other value with one configuration error.                                                                               | Examples for `NaN`, `0`, `-7`, `1.5`, boundaries, and valid integers. PBT: arbitrary strings yield a valid value or typed error. Seed `18802`.                                | Coordinate with the config resolver wave. Risk: existing invalid deployments will fail closed.                   |
| R4  | P1             | `scripts/memory/rollup.ts:381-386` reads `CORE.md` without a missing-file state.                                                                                                                          | Rollup can fail after a model turn when CORE is absent.                | Resume before changing Rollup or CORE bootstrap.                         | `readCore()` distinguishes missing, valid, and unreadable. Missing starts the documented empty state. Unreadable remains an error.                                    | Examples for missing, empty, valid, directory, and permission failure. No PBT.                                                                                                | Prefer after C4 so it reads the canonical directory. Risk: treating all errors as missing would hide corruption. |
| C4  | P1             | `scripts/build.ts:48` uses a logical-OR fallback, while `scripts/poller/config.ts:17` and `scripts/memory/rollup.ts:186` use nullish fallback for `ASSISTANT_DATA_DIR`. Empty input resolves differently. | CLI, agent, Bridge, and memory jobs can write different state trees.   | Resume before any data-dir or process-launch change.                     | One resolver defines trim, empty, relative, and absolute semantics. Every process receives the same canonical path.                                                   | Cross-process examples. PBT: arbitrary strings resolve identically for all consumers. Seed `18804`.                                                                           | Should land before R4 and M3. Risk: path migration can strand existing state.                                    |
| M3  | P1             | `scripts/autograph/graph.py:318-332` and `scripts/memory/brain.ts:133-142` turn corrupt health history into `[]`; Python then overwrites it.                                                              | Historical health data is silently destroyed.                          | Resume after C4 fixes the canonical location.                            | Distinguish missing, valid, and corrupt. Keep corrupt bytes byte-identical. Return non-zero from Graph. Send one rate-limited actionable alert. Use a durable writer. | Examples for missing, valid, truncated, invalid UTF-8, and wrong schema. PBT: arbitrary corrupt bytes remain unchanged. Fault tests around durable replace.                   | Depends on C4 and a canonical durable JSON writer. Risk: strict schema can reject legacy history.                |
| M6  | P1             | `scripts/poller/control.ts:214-217` returns boolean, while `scripts/lib/menu/core.ts:46-50` and `:188-199` discard result and exceptions.                                                                 | UI reports success when delivery failed.                               | Resume before changing CORE flow or delivery statuses.                   | Return `delivered`, `rejected`, or `failed`. Show success only for `delivered`. Preserve answers and offer retry otherwise.                                           | Examples for all three results, throw, retry, and duplicate retry. No PBT.                                                                                                    | Reuse the accepted Bridge ownership result. Risk: retry can duplicate work without a stable key.                 |
| D7  | P2; time-bound | `scripts/autograph/engine.py:55-66` prefers old `last_accessed` over newer `updated`. Explicit touch exists, so the original absolute claim was too broad.                                                | Freshly edited cards can decay into archive.                           | Do not defer longer than a few weeks. Resume before decay-policy tuning. | Define one `effective_activity` contract. It never predates `updated`. Explicit touch remains meaningful.                                                             | Examples for updated-only, touched, future, missing, and malformed dates. PBT: effective activity is never older than valid `updated`. Seed `18707`.                          | Needs owner agreement on activity precedence. Risk: changed decay reshuffles many cards.                         |
| R3  | P2             | `agent/hooks/transcript.ts:6-16` ignores channel kind, while `scripts/memory/rollup.ts:173-176` uses an HTTP client.                                                                                      | Rollup HTTP responses enter the raw conversation transcript.           | Resume before adding another non-Telegram final-response client.         | Persist only approved Telegram final events. Keep internal HTTP results out of raw transcript.                                                                        | Examples for Telegram final, HTTP final, tool event, and missing channel. No PBT.                                                                                             | Independent. Risk: a narrow filter can omit a legitimate transcript source.                                      |
| C2  | P2             | `scripts/poller/update-flow.ts:147-168` deletes update jobs by filesystem `mtime`, although jobs carry `startedAt`.                                                                                       | Clock or copy changes can delete a live job or retain stale state.     | Resume before changing update-job cleanup.                               | Use validated `startedAt`. Quarantine or retain invalid jobs for doctor. Never use `mtime` as authority.                                                              | Examples for valid, invalid, future, and absent `startedAt`. PBT: arbitrary mtimes never change the decision for the same job.                                                | Depends on a quarantine ownership rule. Risk: retained invalid jobs can grow disk use.                           |
| M2  | P2             | `scripts/autograph/supersede.py:45-48` catches every read error and silently removes that Card from candidates.                                                                                           | A damaged Card disappears from maintenance without an alert.           | Resume before changing Supersede selection.                              | Reads return Card or structured skip. Reports list skipped paths. Emit a rate-limited alert. Never rewrite the skipped file.                                          | Examples for invalid UTF-8, malformed frontmatter, permission error, and valid Card. PBT: arbitrary unreadable bytes produce structured skip or typed error, not a raw crash. | Coordinate with M3 alert policy. Risk: noisy alerts on expected transient files.                                 |
| M8  | P2             | `scripts/lib/notice-policy.ts:417-428` checks state, sends, then writes state. Two concurrent callers can both send.                                                                                      | Users can receive duplicate alerts.                                    | Resume before enabling concurrent Brain or removing its systemd lock.    | Claim cross-process ownership before send. Exactly one live claimant sends. Failed send releases or records retry debt.                                               | Deterministic interleaving test and child-process contention test. No PBT.                                                                                                    | Needs a small lock/claim contract, not M19 reuse by default. Risk: a stuck claim can suppress alerts.            |
| M15 | P2             | `agent/lib/telegram-format.ts:398-414`, `agent/lib/card-index.ts:67-72`, and `agent/lib/error-humanizer.ts:42-54` can split UTF-16 surrogate pairs.                                                       | Telegram text, card previews, or errors can contain invalid Unicode.   | Resume before changing those truncation paths.                           | One small cut helper preserves limits and never creates lone surrogates. `core-clamp` behavior remains unchanged.                                                     | Examples around emoji boundaries. PBT: arbitrary strings never yield lone surrogates and never exceed the limit. Seed `18715`.                                                | Independent. Risk: changing units from code units can alter visible limits.                                      |

### Deferred product and cleanup decisions

| ID  | Priority    | Confirmed problem and evidence                                                                                                                    | User impact                                                                  | Return condition                                                     | Acceptance criteria                                                                                                                   | Required tests                                                                                                                    | Dependencies and change risk                                                                |
| --- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| C1  | Owner; P3   | `agent/agent.ts:23-26` sets Eve `sessionTimeoutMs: false`. Stored-session deletion was not proved, but upstream completion lifecycle is disabled. | Long sessions and completion behavior lack one product contract.             | The owner defines desired long-session completion and retention.     | Document the contract. Use upstream lifecycle unless a measured product requirement rejects it. Do not build a local lifecycle first. | Lifecycle examples after the decision. No PBT until state transitions are defined.                                                | Owner decision required. Risk: timeout changes can interrupt active users.                  |
| C3  | P3          | `resolveLang` is duplicated at `agent/lib/i18n.ts:41`, `agent/instructions/05-language.ts:21`, and `agent/instructions/now.ts:17`.                | Copies can drift, but no current data loss is proved.                        | Resume only after a shared import is proven under current Eve.       | Both dynamic instruction modules import one helper. Build and authored-tree guards pass. Behavior stays byte-compatible.              | Shared-import example, `npm run build`, authored-tree guard, and turn-start tests. No PBT.                                        | Requires Eve loader proof. Risk: dynamic module loading can reject the shared import.       |
| C5  | P3          | Glob conversion and walking are duplicated at `agent/tools/glob.ts:12,47` and `agent/tools/grep.ts:11,43`.                                        | Matcher, sorting, or unreadable-directory behavior can drift.                | Resume only after both tools load a shared module under current Eve. | Both tools import one walker and matcher. Current matching, sorting, and unreadable-directory behavior remains exact.                 | Shared-import load test plus matcher, sorting, and unreadable-directory examples. No new PBT unless matcher grammar is specified. | Requires Eve loader proof. Risk: consolidation can change path semantics.                   |
| M4  | P3; blocked | `scripts/autograph/dedup.py:377-383` applies a documented `1.5x` enrichment policy. The review did not reproduce conflict-field data loss.        | A speculative change could replace the canonical body or alter merge policy. | Do not change M4 without a concrete data-loss counterexample.        | First approve a field-policy table. Then preserve every field classified as loss-sensitive in the reproduced case.                    | Keep the concrete counterexample as an example test. Add PBT only after the field algebra is explicit.                            | Blocked by missing evidence and owner policy. Risk: an unproved fix creates real data loss. |

## 5. Dependencies and future order

1. Record owner decisions for S1 and C1. Keep S5-S8 closed unless the owner reopens them.
2. Land M19 before D11 and D12.
3. Finish D11, D12, and D13. Finish M20 before a public installer release.
4. Close S2 and S4 without changing the accepted S5-S8 risks.
5. Land the canonical configuration resolver wave: C4, R1, R2, then R4.
6. Close M3 and M6 against the canonical state and accepted Bridge result.
7. Close D7 within a few weeks. Then take R3, C2, M2, M8, and M15.
8. Take S3, C3, and C5 only when their stated contracts exist.
9. Keep M4 unchanged until a concrete data-loss counterexample exists.

## 6. Non-goals

- Do not change production code during this safe-stop.
- Do not reopen accepted D3, D4, D9, D10, M9-M11, M16, M17, or X1 work.
- Do not refactor duplicate helpers before the shared-import tests pass.
- Do not broaden a boundary without its owner decision.
- Do not turn S5-S8 into implementation tasks without a new ADR.
- Do not add PBT where the invariant is not explicit.

## 7. Cross-wave risks

- Lock and takeover changes can deadlock updates or admit concurrent owners.
- Filesystem generation changes can strand the only active path.
- Canonical path changes can split or strand existing state.
- Strict state classification can reject legacy bytes and needs explicit migration.
- Boundary changes can break documented host or group behavior.
- Installer pinning can fail across platforms without an artifact matrix.
- D10 retained debt can increase disk use until a proven safe collector exists.

## 8. Temporary operating rules

- Do not run two updaters at once.
- Do not run repair.
- Do not perform new public installations.
- Use the Telegram menu only in a private chat.
- Never expose `.env` or bearer values in chat or test output.
- Do not change timezone or context-window settings by hand.

These rules reduce exposure. They do not close the deferred findings.
